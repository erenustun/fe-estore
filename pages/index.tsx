import { Button, Container } from '@component'

const Home = () => {
  return (
    <Container className="py-20">
      <span>Home</span>
      <Button>Home</Button>
    </Container>
  )
}

export default Home
